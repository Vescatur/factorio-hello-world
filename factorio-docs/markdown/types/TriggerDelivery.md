# TriggerDelivery

_type_

Loaded as one of the [TriggerDeliveryItem](../types/TriggerDeliveryItem.md) extensions, based on the value of the `type` key.

**Definition:** [InstantTriggerDelivery](../types/InstantTriggerDelivery.md) — Loaded when the `type` is `"instant"`. ∣ [ProjectileTriggerDelivery](../types/ProjectileTriggerDelivery.md) — Loaded when the `type` is `"projectile"`. ∣ [BeamTriggerDelivery](../types/BeamTriggerDelivery.md) — Loaded when the `type` is `"beam"`. ∣ [StreamTriggerDelivery](../types/StreamTriggerDelivery.md) — Loaded when the `type` is `"stream"`. ∣ [ArtilleryTriggerDelivery](../types/ArtilleryTriggerDelivery.md) — Loaded when the `type` is `"artillery"`. ∣ [ChainTriggerDelivery](../types/ChainTriggerDelivery.md) — Loaded when the `type` is `"chain"`. ∣ [DelayedTriggerDelivery](../types/DelayedTriggerDelivery.md) — Loaded when the `type` is `"delayed"`.
