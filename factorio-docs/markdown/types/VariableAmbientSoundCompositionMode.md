# VariableAmbientSoundCompositionMode

_type_

Defines how are individual samples selected and played after each other.

**Definition:** `"randomized"` ∣ `"semi-randomized"` ∣ `"shuffled"` ∣ `"layer-controlled"`
