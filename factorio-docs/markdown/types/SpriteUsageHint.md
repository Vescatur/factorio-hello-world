# SpriteUsageHint

_type_

Provides hint to sprite atlas system, so it can pack sprites that are related to each other to the same sprite atlas.

**Definition:** `"any"` ∣ `"mining"` ∣ `"tile-artifical"` ∣ `"corpse-decay"` ∣ `"enemy"` ∣ `"player"` ∣ `"train"` ∣ `"vehicle"` ∣ `"explosion"` ∣ `"rail"` ∣ `"elevated-rail"` ∣ `"air"` ∣ `"remnant"` ∣ `"decorative"`
