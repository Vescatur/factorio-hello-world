# Dependencies for tools/svg-to-png.py.
#   pip install -r tools/requirements.txt
#
# resvg-py ships an abi3 wheel for every platform we care about (including
# cp310-abi3-win_amd64, which works on 3.10 through 3.14) and needs no native
# cairo/GTK libraries. tools/factorio-docs-to-md.py remains stdlib-only.
resvg-py>=0.4.0
